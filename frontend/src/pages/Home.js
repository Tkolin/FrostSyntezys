import React from "react";
import LoginForm from "./LoginForm";

const Home = () => {
  return (
    <div style={{ display: "flex", height: "100vh", gap: "5px" }}>
      <div style={{ width: "100%", background: "grey" }}>
        <LoginForm />
      </div>

      <div style={{ width: "100%", background: "grey" }}>sfasf</div>
    </div>
  );
};

export default Home;
